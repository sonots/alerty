log_path: STDOUT
log_level: debug
timeout: 10
lock_path: log/lock
plugins:
  - type: stdout
  - type: file
    path: log/<%= ENV['FILE'] || 'file' %>.log
  - type: exec
    command: cat > log/exec.log
